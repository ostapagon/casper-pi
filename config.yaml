# Casper Pi Voice Assistant Configuration

provider:
  name: gemini

audio:
  sample_rate: 16000
  channels: 1
  chunk_size: 1024
  input_device: null
  output_device: null

wake_word:
  keyword: computer
  sensitivity: 0.5

assistant:
  conversation_timeout: 30
  max_conversation_length: 100

